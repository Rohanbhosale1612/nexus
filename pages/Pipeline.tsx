// This component has been consolidated into Leads.tsx to provide a unified workspace.
// Keeping file as a placeholder to avoid breaking any lingering imports, but it should not be used directly.
import React from 'react';
import Leads from './Leads';

const Pipeline = () => {
  return <Leads />; 
};

export default Pipeline;